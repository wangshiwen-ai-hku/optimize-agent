# 功能特性

## 🎓 Tutor 模式

### 核心功能
- ✅ 智能问答系统
- ✅ 多格式材料支持（文本、PDF）
- ✅ 自动分块和索引
- ✅ 双重检索（关键词 + 语义）
- ✅ **连续工作模式** - 自动化工作流程

### 连续工作模式 ⭐ NEW

**问题**：之前 Agent 会中途停下来说"让我查看..."然后等待用户

**解决**：现在 Agent 会自动连续调用工具直到完整回答

**示例**：
```
用户: 找出文档中所有关键的方程组

Agent 自动执行:
1. semantic_search("方程组") 
2. get_chunk_by_id(13)
3. keyword_search("equation")
4. get_chunk_by_id(25)
5. semantic_search("作用")
...
最后生成完整回答
```

**特点**：
- 🔄 自动连续工作
- 🚫 不会中途停止
- 📊 最多 8-10 次工具调用
- ✅ 完整详细的回答

### 检索工具

| 工具 | 用途 | 适用场景 |
|------|------|---------|
| keyword_search | 关键词搜索 | 查找特定术语、定义 |
| semantic_search | 语义搜索 | 查找相关概念、主题 |
| get_page_content | 获取页面 | 查看特定页面内容 |
| get_chunk_by_id | 获取文本块 | 查看搜索结果详情 |

### 材料处理

**支持格式**：
- 📄 文本文件（.txt, .md）
- 📕 PDF 文档（文本 PDF）

**处理流程**：
```
PDF 文件
    ↓
文本提取
    ↓
智能分块（1000 字符/块，200 字符重叠）
    ↓
双重索引
    ├─ 关键词索引（精确匹配）
    └─ 向量索引（语义搜索）
    ↓
准备就绪
```

### 性能指标

| 指标 | 数值 |
|------|------|
| 小文本文件加载 | < 1 秒 |
| 中型 PDF 加载 | 5-15 秒 |
| 关键词搜索 | < 0.1 秒 |
| 语义搜索 | 1-3 秒 |
| 简单问题回答 | 3-5 秒 |
| 复杂问题回答 | 15-30 秒 |

## 🔍 Solver 模式

### 核心功能
- ✅ 自动问题分析
- ✅ 数学建模
- ✅ 代码生成
- ✅ 自动执行
- ✅ 错误修复（最多 3 次）

### 支持的问题类型

| 类型 | 说明 | 示例 |
|------|------|------|
| LP | 线性规划 | 最大化利润 |
| IP/MIP | 整数规划 | 背包问题 |
| NLP | 非线性规划 | 最小化距离 |
| 凸优化 | 凸函数优化 | 投资组合 |

### 工作流程

```
问题描述
    ↓
问题分析（识别类型）
    ↓
数学建模（变量、目标、约束）
    ↓
代码生成（Python + 求解库）
    ↓
代码执行
    ↓
成功 → 返回结果
失败 → 自动修复 → 重试（最多 3 次）
```

### 使用的库

- **SciPy**: 科学计算和优化
- **CVXPY**: 凸优化
- **PuLP**: 线性规划
- **NumPy**: 数值计算

## 🛠️ 技术特性

### 架构设计

```
┌─────────────────────────────────────┐
│     LangGraph 工作流                │
│  (状态管理 + 节点路由)               │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Gemini 2.5 Flash                │
│  (LLM + Function Calling)           │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     工具层                           │
│  (检索 + 执行 + 分析)                │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     服务层                           │
│  (PDF + Vector + Material)          │
└─────────────────────────────────────┘
```

### 核心技术

| 技术 | 用途 |
|------|------|
| LangGraph | 工作流编排 |
| Gemini 2.5 Flash | 大语言模型 |
| Gemini Embedding | 文本向量化 |
| PyPDF2 | PDF 处理 |
| NumPy | 向量计算 |
| Function Calling | 工具调用 |

### 智能特性

1. **自适应检索**
   - Agent 自动选择关键词或语义搜索
   - 根据结果质量调整策略

2. **连续工作**
   - 自动多次调用工具
   - 无需用户干预
   - 智能判断何时停止

3. **错误恢复**
   - 代码执行失败自动修复
   - 最多 3 次反思机会
   - 详细错误提示

4. **上下文管理**
   - 保持对话历史
   - 工具调用记录
   - 状态持久化

## 📊 使用统计

### 功能覆盖

| 功能 | 状态 | 完成度 |
|------|------|--------|
| 文本材料支持 | ✅ | 100% |
| PDF 材料支持 | ✅ | 100% |
| 关键词搜索 | ✅ | 100% |
| 语义搜索 | ✅ | 100% |
| 连续工作模式 | ✅ | 100% |
| 线性规划求解 | ✅ | 100% |
| 整数规划求解 | ✅ | 100% |
| 非线性规划求解 | ✅ | 100% |
| 代码自动修复 | ✅ | 100% |
| 多轮对话 | ✅ | 100% |

### 限制

| 项目 | 限制 | 说明 |
|------|------|------|
| PDF 大小 | < 50MB | 建议值 |
| 文本提取 | 仅文本 PDF | 不支持扫描版 |
| 工具调用 | 最多 8-10 次 | 避免无限循环 |
| 代码修复 | 最多 3 次 | 避免过度尝试 |
| 并发处理 | 单线程 | 顺序处理 |

## 🎯 使用场景

### Tutor 模式

1. **学习新概念**
   ```
   问: 什么是凸优化？
   → Agent 搜索材料并解释
   ```

2. **查找具体信息**
   ```
   问: primal-dual 方法的算法步骤是什么？
   → Agent 找到相关章节并列出步骤
   ```

3. **理解复杂内容**
   ```
   问: 解释文档中的关键方程组
   → Agent 多次检索并综合说明
   ```

### Solver 模式

1. **生产计划**
   ```
   问: 如何安排生产使利润最大？
   → Agent 建模并求解
   ```

2. **资源分配**
   ```
   问: 如何分配有限资源？
   → Agent 生成优化方案
   ```

3. **路径规划**
   ```
   问: 最短路径问题
   → Agent 建模并计算
   ```

## 🚀 快速开始

### 5 分钟上手

```bash
# 1. 安装
pip install -r requirements.txt

# 2. 配置
echo "GOOGLE_API_KEY=your_key" > .env

# 3. 测试
python demo.py

# 4. 使用 Tutor
python -m src.agent.main tutor your_material.pdf

# 5. 使用 Solver
python -m src.agent.main solver
```

### 示例代码

```python
import asyncio
from src.agent.main import run_tutor, run_solver

# Tutor 示例
async def tutor_example():
    answer = await run_tutor(
        question="什么是线性规划？",
        materials=["textbook.pdf"]
    )
    print(answer)

# Solver 示例
async def solver_example():
    result = await run_solver(
        problem="最大化 z = 3x + 2y，约束：x + y <= 4"
    )
    print(result["solution"])

asyncio.run(tutor_example())
```

## 📖 更多文档

- [快速开始](QUICKSTART.md)
- [Tutor 使用指南](docs/TUTOR_GUIDE.md)
- [连续工作模式](docs/CONTINUOUS_MODE.md)
- [PDF 检索设计](docs/PDF_RETRIEVAL.md)
- [项目结构](PROJECT_STRUCTURE.md)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License
